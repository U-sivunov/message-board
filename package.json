{
  "name": "message-board",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"cd frontend && npm install && npm start\" \"cd backend  && npm install && npm start\"",
    "install": "concurrently \"cd frontend && npm install\" \"cd backend && npm install\"",
    "build": "concurrently \"cd frontend && npm run build\" \"cd backend && npm install && npm start\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "devDependencies": {
    "create-react-app": "^5.0.1"
  }
}
